declare module './server/routes/admin-services' {
  import type { Router } from 'express';
  const router: Router;
  export default router;
}

declare module './server/routes/admin-products' {
  import type { Router } from 'express';
  const router: Router;
  export default router;
}

declare module './server/routes/admin-testimonials' {
  import type { Router } from 'express';
  const router: Router;
  export default router;
}

declare module './server/routes/admin-blog' {
  import type { Router } from 'express';
  const router: Router;
  export default router;
}

declare module './server/routes/admin-quotes' {
  import type { Router } from 'express';
  const router: Router;
  export default router;
}

declare module './server/routes/admin-contacts' {
  import type { Router } from 'express';
  const router: Router;
  export default router;
}
